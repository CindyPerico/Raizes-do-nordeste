spring:
  datasource:
    url: jdbc:h2:mem:raizes-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: chave-de-teste-com-no-minimo-32-caracteres-para-hmac
    expiration-ms: 3600000
  seed:
    enabled: true
